"""LLM-аналитик аудита: думающий слой поверх собранных сигналов.

Детерминированный код собирает кандидатов (сигналы с фактами), а вердикт выносит
DeepSeek: проблема / норма / непонятно, плюс объяснение «что, почему, последствия»
и предложения по исправлению. Код не решает нюансные случаи сам.
"""

import json

from core.logger import logger
from services.audit.humanize import humanize_money
from services.audit.team_context import COMPANY_DESCRIPTION, SPEAK_RULES, TEAM_CONTEXT
from services.llm_service import LLMClient

_SYSTEM = (
    f'Ты — опытный аудитор складского и финансового учёта в системе МойСклад '
    f'для {COMPANY_DESCRIPTION}. '
    f'Учёт ведут несколько сотрудников под одним аккаунтом, ошибки — человеческий фактор.\n\n'
    'Тебе дают СИГНАЛ от автоматической проверки: подозрение на ошибку с фактами из API. '
    'Твоя задача — вынести суждение, как это сделал бы въедливый главбух:\n'
    '1. verdict: "problem" (это ошибка учёта), "ok" (это норма, ложная тревога), '
    '"unclear" (нужно спросить человека).\n'
    '2. explanation: 1-3 коротких предложения — ЧТО не так и НА ЧТО влияет '
    '(себестоимость / остатки / балансы). Только факты и вывод; без рассуждений '
    'вида «это может означать», без пересказа сумм и номеров, уже видных в карточке.\n'
    '3. suggestions: 1-3 варианта, каждый — ОДНА короткая строка «что сделать — что даст». '
    'Без слова «Вариант:», без своей нумерации (её добавит интерфейс), без пересказа '
    'правил. Если в фактах есть fix_hint — варианты обязаны следовать ему, '
    'а не противоречить.\n\n'
    'Контекст компании:\n'
    '- Себестоимость считается FIFO; нулевые цены в приёмках/оприходованиях занижают '
    'себестоимость готовой продукции. Часть этикеток поставщик печатает БЕСПЛАТНО — '
    'для них нулевая цена корректна, если это указано в комментарии.\n'
    '- Стандарт: платёж за доставку от поставщика НЕ привязывается к заказу, '
    'сумма идёт в накладные расходы приёмки.\n'
    '- Стандарт: платёж за доставку до покупателя — ВСЕГДА отдельный исходящий '
    'платёж/РКО с галкой «Без закрывающих документов», БЕЗ привязки к отгрузке '
    'или заказу (привязка ломает баланс контрагента — это не деньги за товар). '
    'НИКОГДА не предлагай привязывать такие платежи; связь с отгрузкой — только '
    'через сумму и комментарий. Для доставки СДЭК парного платежа не бывает — '
    'СДЭК выставляет сводный счёт за период.\n'
    '- Покупки через оприходование допустимы (сотрудник купил сам), но должны иметь '
    'комментарий с источником и реальную цену.\n'
    '- Разборка готовой продукции обратно в основу = пара списание+оприходование '
    'со взаимными ссылками; оприходование по текущему FIFO.\n'
    '- Нулевые отгрузки бывают намеренно (подарки) — тогда причина обязана быть в комментарии.\n'
    '- Поздние правки заказов покупателей и отгрузок — норма процесса (статусы, оплаты). '
    'Правка проведённого складского документа или выполненного ПЗ спустя сутки и позже — '
    'проблема (пересчитывает FIFO и остатки прошлых дат). Говори «поздняя правка» или '
    '«правка старого документа», НЕ «задним числом» — задним числом это про дату документа, '
    'а не про время правки.\n'
    '- Комментарии обычно начинаются с имени сотрудника.\n'
    f'{TEAM_CONTEXT}\n{SPEAK_RULES}\n\n'
    'ВАЖНО про числа в фактах: суммы и цены УЖЕ переведены в рубли («0,45 ₽») — '
    'НЕ дели их ни на что, показывай как есть. Цена и себестоимость — за ОДНУ '
    'единицу измерения товара (поле uom: г, мл, шт), не за килограмм и не за литр; '
    'единицу в тексте бери только из uom, не подставляй свою.\n\n'
    'Если в фактах есть история изменений (changes_after_doc_date / '
    'changes_after_completion) — ОБЯЗАТЕЛЬНО перескажи в explanation, ЧТО именно '
    'менялось, по-русски: какие поля, старое → новое значение, когда '
    '(например: «изменено количество материала Основа шампуня: 10 → 12, статус '
    'переведён из „готово" в „выполнение"»). Не пиши «зафиксирован факт правки» '
    'без содержания. Если история пуста — так и скажи: «что именно менялось, '
    'по истории не видно» и предложи открыть документ.\n'
    'ВАЖНО: комментарий документа (поле description в фактах) — главный источник контекста. '
    'Всегда читай его ПЕРЕД вердиктом: сотрудники пишут туда причину '
    '(«бесплатно», «подарок», «частичная приёмка», «см. инвентаризацию»). '
    'Если комментарий объясняет ситуацию — это часто «ok»; '
    'если противоречит данным или отсутствует там, где обязана быть причина — «problem». '
    'В suggestions ссылайся на комментарий, когда он есть.\n\n'
    'Отвечай СТРОГО одним JSON-объектом без markdown:\n'
    '{"verdict": "problem|ok|unclear", "severity": "critical|important|warning", '
    '"explanation": "...", "suggestions": ["Вариант: ... Последствие: ...", ...]}\n'
    'Пиши по-русски, конкретно, без воды. Числа и номера документов — из фактов, не выдумывай.'
)


class AuditAnalyst:
    def __init__(self, llm: LLMClient | None = None):
        self.llm = llm or LLMClient()

    async def triage(self, check_title: str, facts: dict) -> dict | None:
        """Вернуть {'verdict', 'severity', 'explanation', 'suggestions'} или None при сбое LLM."""
        # деньги переводим в рубли кодом: LLM ошибается в 100 раз, читая копейки
        user_msg = (
            f'Сигнал проверки: {check_title}\n'
            f'Факты (JSON из МойСклад API, суммы уже в рублях):\n'
            f'{json.dumps(humanize_money(facts), ensure_ascii=False, default=str)}'
        )
        try:
            resp = await self.llm.chat([
                {'role': 'system', 'content': _SYSTEM},
                {'role': 'user', 'content': user_msg},
            ])
            content = (resp.get('content') or '').strip()
            start, end = content.find('{'), content.rfind('}')
            if start == -1 or end == -1:
                logger.warning(f'[analyst] не-JSON ответ: {content[:200]}')
                return None
            data = json.loads(content[start:end + 1])
            if data.get('verdict') not in ('problem', 'ok', 'unclear'):
                return None
            data.setdefault('severity', 'important')
            data.setdefault('suggestions', [])
            return data
        except Exception as e:
            logger.warning(f'[analyst] triage не удался: {e}')
            return None
