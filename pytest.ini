[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: all unit tests
    finance: business logic (transactions, calculations)
    validation: input validation (amounts, formats)
    fsm: FSM workflow tests (add operation, edit, categories)
    access: admin-only access control
    audit: MoySklad accounting audit checks and runner
